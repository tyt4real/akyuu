# Placeholder CSAM hash blocklist: one hex SHA-256 per line, '#' comments allowed.
# Any file whose content hash appears here is discarded before storage, every
# time full-res download is enabled. See docs/ for how hashes are computed.